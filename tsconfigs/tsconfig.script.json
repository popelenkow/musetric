{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"],
    "rootDir": "${configDir}",
    "tsBuildInfoFile": "${configDir}/tsconfig.script.tsbuildinfo"
  },
  "include": ["${configDir}/**/*.ts"],
  "exclude": ["${configDir}/src/**/*", "${configDir}/dist/**/*"]
}
